import month from './Months.js';
import weekday from './Weekdays.js';

/** @typedef {import('../../types').LocaleTime} LocaleTime */

const time = /** @type LocaleTime */ ({
  month,
  weekday,
});

export default time;
